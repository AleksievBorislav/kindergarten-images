# Ръководство по настройка - Детска Градина Уебсайт

## Етап 1: Инициализиране на Git репозиторий

1. Отворете терминал в папката на проекта:
   ```bash
   cd c:\Users\Borislav\vscode\ site
   ```

2. Инициализирайте git репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit: kindergarten website"
   ```

## Етап 2: Създаване на GitHub репозиторий

1. Отворете [GitHub.com](https://github.com) и влезте в акаунта си
2. Кликнете на **+** в горния десен ъгъл и изберете "New repository"
3. Назовете го `vscode-site` (или както искате)
4. Изберете **Public** (за публичен достъп до сайта)
5. НЕ инициализирайте с README (защото вече имаме един)
6. Кликнете "Create repository"

## Етап 3: Свързване на локалния репозиторий с GitHub

Следвайте инструкциите на GitHub:

```bash
git remote add origin https://github.com/YOUR_USERNAME/vscode-site.git
git branch -M main
git push -u origin main
```

Заменете `YOUR_USERNAME` с вашето потребителско име на GitHub.

## Етап 4: Активиране на GitHub Pages

1. Отворете вашия репозиторий на GitHub
2. Отидете на **Settings** (Настройки)
3. На левия панел, кликнете **Pages**
4. Под "Build and deployment", изберете:
   - **Source**: "Deploy from a branch"
   - **Branch**: "main" и "/" (root)
5. Кликнете **Save**
6. Чакайте минута за потвърждението

Вашият сайт ще бъде достъпен на: `https://YOUR_USERNAME.github.io/vscode-site/`

## Етап 5: Редактиране на съдържанието

### Способ 1: Директно на GitHub (препоръчано за начало)

1. Отворете вашия репозиторий на GitHub
2. Отворете папката `data/`
3. Кликнете на желания JSON файл (напр. `articles.json`)
4. Кликнете на иконата на молива (✏️) за редактиране
5. Направете промени
6. Кликнете "Commit changes"

Примерни промени:

**За добавяне на статия в `data/articles.json`:**
```json
{
  "id": 3,
  "title": "Название на новата статия",
  "date": "2026-01-18",
  "content": "Съдържание на статията"
}
```

**За добавяне на снимка в `data/gallery.json`:**
```json
{
  "id": 5,
  "title": "Нова снимка",
  "url": "https://example.com/image.jpg"
}
```

### Способ 2: Локално редактиране

1. На вашия компютър, редактирайте JSON файловете
2. Направете commit:
   ```bash
   git add .
   git commit -m "Update content"
   ```
3. Направете push:
   ```bash
   git push origin main
   ```

Сайтът ще се актуализира автоматично за няколко секунди.

## Етап 6: Тестване локално

За предварителен преглед на сайта преди публикуване:

1. Инсталирайте Python 3:
   ```bash
   python --version
   ```

2. Стартирайте локален сървър:
   ```bash
   python -m http.server 8000
   ```

3. Отворете браузера и отидете на: `http://localhost:8000`

## Как да добавя собствени снимки

Вместо да ползвате placeholder снимки:

1. Качете снимките си на облачен сервис (напр. [Imgur](https://imgur.com), [Cloudinary](https://cloudinary.com))
2. Копирайте URL-а на снимката
3. Редактирайте съответния JSON файл и заменете URL-а

Пример за `data/gallery.json`:
```json
{
  "id": 1,
  "title": "Детско парти",
  "url": "https://i.imgur.com/example123.jpg"
}
```

## Безопасност и контрол на достъпа

- Вашият репозиторий е **частен** по подразбиране (ако го направите такъв)
- Само вие (или хора, които поканите) можете да направите промени
- Всяка промяна е записана в git историята
- GitHub Pages е статично хостване - няма бази данни или риск от атаки

## Решаване на проблеми

### Сайтът не се актуализира след промени

1. Стойте 30-60 секунди за GitHub Pages да преразвърта сайта
2. Натиснете Ctrl+Shift+Delete в браузера за почистване на кеша
3. Посетете сайта в режим на безличния преглед (Ctrl+Shift+Delete)

### JSON файловете не са валидни

1. Проверете дали всички скобки и кавички са правилни
2. Ползвайте [JSONLint](https://jsonlint.com/) за валидиране

### GitHub Pages не работи

1. Проверете дали репозиторият е публичен
2. Проверете дали Pages е активиран в Settings
3. Проверете дали има .github/workflows/deploy.yml файл

## Поддържащи ресурси

- [GitHub Pages документация](https://pages.github.com/)
- [JSON формат](https://www.json.org/)
- [HTML/CSS справка](https://developer.mozilla.org/en-US/)

Готово! Вашият сайт е жив и готов за редактиране! 🎉
